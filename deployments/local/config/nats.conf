# NATS Server Configuration
jetstream {
  store_dir: "/data"
}

# Enable MQTT support
mqtt {
  port: 1883

  # Map MQTT topics to NATS subjects
  # MQTT topic pattern -> NATS subject
  # This allows MQTT clients to publish to NATS JetStream
}
